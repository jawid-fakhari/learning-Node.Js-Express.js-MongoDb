<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/style.css">
    <!-- <style>
        nav {
            background-color: #333;
            padding: 10px 20px;
            color: #fff;
        }

        /* Stile per il titolo del sito */
        .site-title {
            margin-bottom: 10px;
        }

        .site-title a {
            text-decoration: none;
            color: #fff;
        }

        .site-title h1 {
            margin: 0;
            font-size: 24px;
        }

        .site-title p {
            margin: 0;
            font-size: 14px;
        }

        /* Stile per i link nella barra di navigazione */
        ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        ul li {
            display: inline;
            margin-right: 10px;
        }

        ul li a {
            text-decoration: none;
            color: #fff;
            font-size: 16px;
        }

        ul li a:hover {
            color: #ffd700;
            /* Cambia colore al passaggio del mouse */
        }

        /* Stile per il modulo di creazione del blog */
        .create-blog {
            background-color: #f9f9f9;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .create-blog form {
            display: flex;
            flex-direction: column;
        }

        .create-blog label {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .create-blog input,
        .create-blog textarea {
            padding: 8px;
            margin-bottom: 10px;
            border-radius: 4px;
            border: 1px solid #ccc;
        }

        .create-blog button {
            padding: 10px 20px;
            background-color: #333;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .create-blog button:hover {
            background-color: #555;
        }

        /* Stile per la sezione dei blog */
        .blogs {
            padding: 20px;
        }

        .blogs h2 {
            font-size: 24px;
            margin-bottom: 10px;
        }

        .blogs .title {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .blogs .snippet {
            font-size: 16px;
            margin-bottom: 15px;
        }

        /* Stile per il messaggio di nessun blog */
        .blogs p {
            font-size: 16px;
            color: #777;
        }

        footer {
            background-color: #333;
            color: #fff;
            padding: 20px 0;
            text-align: center;
        }

        footer p {
            margin: 0;
            font-size: 14px;
        }
    </style> -->
</head>